{{define 'is_local_dev' true ~}}
{{define 'service_name' (hyphen service.name) ~}}
**

!/src/**
!build.gradle
!gradle.properties
!settings.gradle
!entrypoint.sh
{{#if 'is_local_dev' ~}}
!/build/libs/{{service_name}}*.jar
{{/if}}
