package {{entity.namespace}}.entity.{{lower-snake entity.name}}

import org.springframework.stereotype.Component
import org.springframework.data.elasticsearch.core.ReactiveElasticsearchOperations

import java.util.concurrent.CompletableFuture
import java.util.*

interface {{upper-camel entity.name}}Searcher {
    /**
     * Finds {{plural entity.name}} which matches the given conditions.
     */
    {{define 'class_name' (upper-camel entity.name) ~}}
    fun find{{plural class_name}}(input: {{class_name}}SearchInput = {{class_name}}SearchInput()): CompletableFuture<List<{{class_name}}Document>>
}