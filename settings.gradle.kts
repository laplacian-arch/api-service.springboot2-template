rootProject.name = "laplacian-arch.template.api-server.springboot"
pluginManagement {
    repositories {
        maven(url = "../mvn-repo/")
        maven(url = "https://raw.github.com/nabla-squared/mvn-repo/master/")
        gradlePluginPortal()
        jcenter()
    }
}