{{#define "service_name"}}{{hyphen service.identifier}}{{/define}}
#!/usr/bin/env bash
set -e
DIR=$(cd $(dirname "$0") && pwd)

set -x
source $DIR/{{service_name}}-service-image-settings.sh

docker tag $TAG_CURRENT $TAG_LATEST

docker push $TARGET
